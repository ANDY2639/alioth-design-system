# 🏗️ Alioth Design System - Architecture Diagram

## System Architecture Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        Browser (Client)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            layout.tsx (Root Layout)                       │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │  <ColorProvider>  ← Wraps entire app               │ │   │
│  │  │  ┌───────────────────────────────────────────────┐ │ │   │
│  │  │  │          localStorage Manager                 │ │ │   │
│  │  │  │  ↓ Persists theme selection                   │ │ │   │
│  │  │  │  Key: 'alioth-color-theme'                    │ │ │   │
│  │  │  │  Value: 'primary' | 'secondary' | ...         │ │ │   │
│  │  │  └───────────────────────────────────────────────┘ │ │   │
│  │  │  ┌───────────────────────────────────────────────┐ │ │   │
│  │  │  │   <DashboardContent>  'use client'            │ │ │   │
│  │  │  │  ┌──────────────────────────────────────────┐ │ │ │   │
│  │  │  │  │        Sidebar Navigation                │ │ │ │   │
│  │  │  │  │  • Colors (activeSection='colors')       │ │ │ │   │
│  │  │  │  │  • Typography                            │ │ │ │   │
│  │  │  │  │  • Components                            │ │ │ │   │
│  │  │  │  └──────────────────────────────────────────┘ │ │ │   │
│  │  │  │  ┌──────────────────────────────────────────┐ │ │ │   │
│  │  │  │  │  MainLayout (Scrollable Content Area)    │ │ │ │   │
│  │  │  │  │  ┌────────────────────────────────────┐  │ │ │ │   │
│  │  │  │  │  │  Dynamic Sections via Suspense:    │  │ │ │ │   │
│  │  │  │  │  │                                    │  │ │ │ │   │
│  │  │  │  │  │  ┌─────────────────────────────┐  │  │ │ │ │   │
│  │  │  │  │  │  │ Colors Section              │  │  │ │ │ │   │
│  │  │  │  │  │  │  ↓ Suspense boundary        │  │  │ │ │ │   │
│  │  │  │  │  │  │    - ColorPalettePicker    │  │  │ │ │ │   │
│  │  │  │  │  │  │    - ColorCard             │  │  │ │ │ │   │
│  │  │  │  │  │  └─────────────────────────────┘  │  │ │ │ │   │
│  │  │  │  │  │                                    │  │ │ │ │   │
│  │  │  │  │  │  ┌─────────────────────────────┐  │  │ │ │ │   │
│  │  │  │  │  │  │ Typography Section          │  │  │ │ │ │   │
│  │  │  │  │  │  │  ↓ Suspense boundary        │  │  │ │ │ │   │
│  │  │  │  │  │  │    - FontShowcase           │  │  │ │ │ │   │
│  │  │  │  │  │  └─────────────────────────────┘  │  │ │ │ │   │
│  │  │  │  │  │                                    │  │ │ │ │   │
│  │  │  │  │  │  ┌─────────────────────────────┐  │  │ │ │ │   │
│  │  │  │  │  │  │ Components Section          │  │  │ │ │ │   │
│  │  │  │  │  │  │  ↓ Suspense boundary        │  │  │ │ │ │   │
│  │  │  │  │  │  │    - ComponentShowcase      │  │  │ │ │ │   │
│  │  │  │  │  │  └─────────────────────────────┘  │  │ │ │ │   │
│  │  │  │  │  └────────────────────────────────┘  │  │ │ │ │   │
│  │  │  │  └──────────────────────────────────────┘ │ │ │   │
│  │  │  └───────────────────────────────────────────┘ │ │   │
│  │  └─────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Color State Management

```
┌─────────────────────────────────────────────────────────────┐
│              ColorContext.tsx (State Management)              │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  useState('primary')  ← Initial theme from localStorage      │
│       ↓                                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ theme: 'primary' | 'secondary' | ... | 'neutral'   │   │
│  └──────────────────────────────────────────────────────┘   │
│       ↓                                                       │
│  setTheme('newTheme')                                        │
│       ↓                                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 1. Update useState                                  │   │
│  │ 2. Apply CSS variables to <html> element            │   │
│  │ 3. Save to localStorage                             │   │
│  │ 4. Trigger re-render of all components              │   │
│  └──────────────────────────────────────────────────────┘   │
│       ↓                                                       │
│  useColorTheme() - Hook for components                       │
│       ↓                                                       │
│  Returns: { theme, setTheme, currentPalette, colors }       │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## CSS Variables System

```
globals.css
│
├─ :root { --color-50 through --color-950 }
│  (Default: Primary palette)
│
├─ .dark { --color-50 through --color-950 }
│  (Dark mode versions)
│
└─ Theme-specific overrides
   html.theme-secondary {
     --color-50: #ECEFFF;
     --color-100: #DFE0FF;
     ...
     --color-950: #211650;
   }

   html.theme-success {
     --color-50: #F0FDF4;
     ...
   }

   /* All 7 palettes... */
```

### CSS Variable Reference

```
--color-50   → Lightest (backgrounds, disabled states)
--color-100  → Light (hover backgrounds)
--color-200  → Light-mid (secondary backgrounds)
--color-300  → Light-mid-dark
--color-400  → Mid-light (lighter foreground)
--color-500  → BASE COLOR (main UI elements)
--color-600  → Mid-dark (hover states)
--color-700  → Dark (foreground elements)
--color-800  → Darker
--color-900  → Very dark (text, borders)
--color-950  → Darkest (dark backgrounds, text)
```

---

## Component Reactivity Flow

```
User selects new theme in ColorPalettePicker
         ↓
    onClick → setTheme('secondary')
         ↓
    ColorContext updates state
         ↓
    CSS variables applied to <html>
         ↓
    All components using var(--color-XXX) update instantly
         ↓
    ┌─────────────────────────────────────────┐
    │ ColorCard (shows new palette)            │
    │ DemoButton (colors change)               │
    │ DemoBadge (colors change)                │
    │ FontShowcase (accent colors change)      │
    │ All other styled elements update         │
    └─────────────────────────────────────────┘
```

---

## Component Hierarchy

```
layout.tsx (Root)
│
└─ ColorProvider
   │
   ├─ Navbar
   │  └─ ThemeToggle (Dark/Light mode)
   │
   └─ DashboardContent ('use client')
      │
      ├─ Sidebar (Static)
      │  ├─ NavItem: Colors
      │  ├─ NavItem: Typography
      │  └─ NavItem: Components
      │
      └─ MainLayout
         │
         ├─ [Colors Section] (Dynamic)
         │  ├─ Suspense
         │  │  ├─ ColorPalettePicker (Client)
         │  │  │  └─ useColorTheme()
         │  │  │     └─ ColorPalettePicker.tsx
         │  │  │
         │  │  └─ ColorCard (Client)
         │  │     ├─ useColorTheme()
         │  │     └─ ColorSwatch × 11
         │  │        └─ Click → Copy to clipboard
         │  │
         │  └─ LoadingPlaceholder
         │
         ├─ [Typography Section] (Dynamic)
         │  ├─ Suspense
         │  │  └─ FontShowcase (Client)
         │  │     └─ useColorTheme()
         │  │
         │  └─ LoadingPlaceholder
         │
         └─ [Components Section] (Dynamic)
            ├─ Suspense
            │  └─ ComponentShowcase (Client)
            │     ├─ DemoButton (Client)
            │     │  └─ useColorTheme()
            │     ├─ DemoBadge (Client)
            │     │  └─ useColorTheme()
            │     └─ DemoCard (Client)
            │        └─ useColorTheme()
            │
            └─ LoadingPlaceholder
```

---

## Data Flow: Theme Change

```
1. User Acti
```
